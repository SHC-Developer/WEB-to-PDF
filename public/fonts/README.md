`HY신명조`(원래 의도된 폰트)를 모든 PC에서 동일하게 렌더링하려면 폰트 파일을 이 폴더에 넣으세요.

현재는 저작권 이슈로 `HY신명조` 대신 `조선신명조` 계열 폰트를 사용합니다.

필수 파일명:

- `ChosunSm.TTF` (일반/400)
- `ChosunKm.TTF` (굵게/700)

주의:

- 정확히 같은 모양을 유지하려면 모든 PC가 아니라 이 폴더의 동일한 폰트 파일을 사용해야 합니다.
- 상용 폰트일 수 있으므로 배포 전 라이선스를 확인하세요.
